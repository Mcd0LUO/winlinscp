# 变更日志

## [0.6.1] - 2026-08-12

### 变更
- **移除"主机别名"连接方式**：连接对话框只保留「登录脚本 / IP+密码」二选一；脚本内部仍可经 `~/.ssh/config` 别名（含跳板/密钥）连接
- **脚本目标权威化**：连接/自动连接以脚本解析的 Target/WorkDir 为准，自动修复历史保存的脏 HostAlias（此前可编辑别名下拉会把显示串如 `host (root@…)` 写进配置，导致 `Could not resolve hostname …)` 连接失败）
- **移除仓库内个人主机信息**：别名/远端路径/登录脚本绝对路径改为占位 + 环境变量覆盖（`WINLINSCP_HOST` / `WINLINSCP_WORKDIR` / `WINLINSCP_LOGIN_SCRIPT`）

## [0.6.0] - 2026-08-08

### 新增
- **IP + 密码登录配置**：连接对话框新增「IP/主机 + 用户名 + 密码」直接连接（第三种连接方式，无需登录脚本、无需 ~/.ssh/config 别名）；密码经系统 ssh 的 `SSH_ASKPASS` 机制免交互认证（**WinLinScp 自身充当 askpass 助手**，由 ssh 拉起、输出密码后即退出）；列目录/传输/增删改全部走同一 ssh/scp 栈（密码模式自动切 `BatchMode=no` + 强制密码认证）
- 配置 `profiles.json` 支持 `host`/`user`/`password` 字段；自动连接同样适用
- **连接方式改为三选一**：连接对话框顶部「登录脚本 / 主机别名 / IP+密码」单选，只显示所选方式的字段（避免混填误导），保存/连接按所选方式校验
- **顶部菜单栏「关于」**：显示应用图标、名称、版本与功能简介（关于对话框）

### 注意
- 密码以**明文**存于 `profiles.json`（应用目录），请保管好该文件；需要更高安全可考虑后续加主密码加密

## [0.5.1] - 2026-08-08

### 变更
- **传输队列 → 任务队列**：底部栏改名"任务队列"，卡片区可拖高（拖标题栏下分隔线调节高度，可收起/展开）
- **列表顶部".."返回上一级（修复远端缺失）**：远端".."条目名字以 `.` 开头被"隐藏文件"过滤误吞（默认不显示隐藏时远端列表缺".."）→ 改为 `..` 不随隐藏过滤，本地/远端文件列表顶部始终有".."返回上一级

### 新增
- **本地右键菜单**：「以终端打开」（所选文件夹或当前目录，Windows Terminal，不可用回退 cmd）+「以任务管理器打开」（启动 taskmgr.exe）
- **应用图标**：Windows ↔ Linux 握手图标（蓝色圆角方块 + 白色握手，左臂 Windows 四格、右臂 Tux），多尺寸 ICO 内嵌 exe；`tools/icon_gen` 可重新生成

### 优化
- **本地 zip 压缩不再卡 UI**：压缩/解压等本地操作改在线程池执行（原同步占住 UI 线程，大文件夹会卡界面），任务卡后台持续显示进度
- **压缩等级可选并显示**：本地「压缩为 .zip」前弹出压缩等级选择（不压缩 / 最快 / 标准 / 最小体积），任务卡显示所用等级（如「压缩 zip」「名（标准）」）

## [0.5.0] - 2026-08-08

### 新增
- **交互式面包屑地址栏**：左右地址栏由可点击分块构成（本地 `D:` › `game` › `…`，远端 `/` › `home` › `…`），点击任一块直接跳转；**文件可拖到指定块** = 移入/上传/下载到该目录（复用目录行拖放语义，含 linux 内拖动到块）；保留铅笔编辑框（输入路径）+ 历史下拉（访问过的目录 MRU）
- **文件操作 → 显式队列任务**：压缩/解压/删除/重命名/新建/移动/执行脚本等不再静默，在底部传输队列显示任务卡（运行中→完成/失败，可取消）；任务与字节传输共用卡片契约（`ITransferTask`）
- **传输 ETA + 乐观进度条**：文件与文件夹均实时轮询目标已写字节（文件夹=远端 `du -sb` / 本地递归），并发预估总字节 → 确定性进度条（总量未知时保持不确定条），完成前封顶 95%（乐观）；卡片显示"剩余 X 分 Y 秒"
- **多选拖拽修复**：拖拽在按下瞬间快照完整多选集（ListView 收拢为单选之前），一次拖入全部选中项；ListView 显式 `SelectionMode=Extended`
- **批量上传确认 + 打包**：多选上传（拖拽/工具栏）先弹确认框（项数/总字节/目标目录），打包选项**不打包 | tar（默认）| zip**；打包 = 本地打单个归档 → 上传单文件（有真实进度）→ **远端自动解压到目标目录并清理临时归档**（tar 用 .NET `System.Formats.Tar`，zip 用 `ZipArchive`）
- 工具栏/右键"上传"与"下载"改为作用于**全部选中项**（与批量删除一致）

### 性能
- 传输进度轮询扩到文件夹（原来只对单个文件测速）

## [0.4.4] - 2026-08-08

### 修复
- **批量删除只删一个**：多选条目后删除现在删除全部选中项（原只删单个 SelectedItem）；本地与远端面板均支持

## [0.4.3] - 2026-08-08

### 变更
- **移除数据目录 AppData 回退与迁移代码**：数据目录**始终**为 exe 所在目录（不做可写性回退、不依赖 AppData、不做旧配置迁移）。配置 `profiles.json` 与图标缓存 `icons\` 跟随应用文件夹。

## [0.4.2] - 2026-08-08

### 变更
- **数据目录改为 exe 所在目录（便携）**：`profiles.json` 与图标缓存 `icons\` 放在应用自身目录，拷贝整个文件夹即完整迁移

## [0.4.1] - 2026-08-08

### 优化
- **图标清晰度**：shell 图标改取 32px 大图标下采样显示（原 16px 小图标），高 DPI 下更锐利；磁盘缓存同步升级并自动失效旧 16px 缓存
- **改为非单文件发布**：`dist\` 为文件夹形式（exe + dll），启动略快；`build-dist.cmd` 相应更新

## [0.4.0] - 2026-08-08

### 新增
- **目录恢复**：重启后本地面板回到上次退出的本地目录；远端面板回到上次退出的远端目录（按配置区分，避免跨主机串台）
- **图标磁盘缓存**：shell 图标提取后落盘到 `%AppData%\WinLinScp\icons\`，重启后内存→磁盘→shell 三级取图标，免重复 shell 调用

## [0.3.5] - 2026-08-08

### 修复
- **窗口显示不再被预连接阻塞**：预连接/自动连接推迟到窗口显示之后启动（`Task.Yield` 让出 UI 线程），SSH 进程 spawn 等启动工作不再落在 `win.Show()` 之前

## [0.3.4] - 2026-08-08

### 性能
- **启动预连接**：自动连接改为与本地面板**并行**启动（原先串行——等本地枚举完才开始 SSH 建连），SSH 建连(~1s)重叠进本地面板加载时间，右侧不再晚于左侧 1 秒

## [0.3.3] - 2026-08-08

### 性能 / 异步优化
- **图标后台懒加载**：文件行先显示字形，shell 图标（SHGetFileInfo）在后台线程提取、UI 线程创建，UI 线程不再有逐行图标开销；带虚拟化复用防错位保护
- **本地枚举提速**：改用 `EnumerateFileSystemInfos` 一次取回带元数据的条目（原每文件多次系统调用）
- **加载/空目录占位**：面板加载中显示"正在加载…"、空目录显示"（空目录）"，不再空白干等

## [0.3.2] - 2026-08-08

### 修复
- **冷启动自动连接更可靠**：
  - 连接前先 `mkdir -p` 确保工作目录存在（与登录脚本行为一致）
  - 首次列目录失败自动重试一次（冷启动首连偶发失败不再直接报错）
  - 找不到 LastProfileName 对应的配置时给出明确提示（原为静默不连）
  - 探针改用隔离配置目录，不再污染真实 `%AppData%\WinLinScp\profiles.json`

## [0.3.1] - 2026-08-08

### 新增
- **linux 内拖动**：远端文件拖到远端**目录行** = ssh mv 移入该目录（同目录 no-op）；本地拖到目录行同理
- 拖拽到目录行也作用于跨系统：本地拖到远端目录行 = 上传到该目录，远端拖到本地目录行 = 下载到该目录
- **传输队列独立下侧栏**：常驻标题栏（完成数/当前活动/取消全部/清理/收起）+ 横向卡片列表（固定高度不挤压文件区，传输开始自动展开）
- **复用 Windows shell 图标**：`SHGetFileInfo` 取真实文件夹/盘符/文件类型图标（按类型缓存），字形+配色作回退

## [0.3.0] - 2026-08-08

### 新增
- **拖拽支持**：
  - 跨系统：本地文件拖到远端面板 = 上传；远端条目拖到本地面板 = 下载
  - 系统内：本地文件拖入本地面板 = 移动（Ctrl 拖拽 = 复制）
  - 支持从 Windows 资源管理器直接拖文件到面板
- **执行自定义脚本**：右键「执行自定义脚本…」弹出文本输入框；本地面板用 PowerShell（`-Command`），远端面板用 bash（经常驻会话，工作目录=当前目录）；结果弹窗显示输出
- 常驻会话关闭改为优雅退出（关 stdin → 远端循环 EOF 自然退出），避免残留跳板子进程

### 修复（2-agent 并行审计结果）
- **P1** 共享会话：排队等待期间的取消不再误杀正在执行其它命令的会话；帧等待期间取消正确报"已取消"而非"超时"
- **P1** 断开/切换连接时作废在途导航与后台刷新，避免旧服务器列表回填 / 污染新连接缓存
- **P1** 拖拽移动覆盖不再"先删目标再移动"（先备份目标、成功后再删），避免移动失败导致数据丢失
- **P1** 传输启动不再先阻塞统计总字节（并发预估）；已取消项直接跳过不进传输
- **P1** 拒绝上传盘符根目录（原会退化成 `scp -r ./` 上传整个盘）
- **P2** 远端面板"显示隐藏"开关生效（原来只对本地有效）
- **P2** 连接对话框主机别名下拉显示别名（原 `DisplayMemberPath` 指向不存在属性导致空白）
- **P2** 临时文件加随机前缀防同名互覆盖；`_navCts` 及时释放；拖拽路径校验 + 排除 `..` 项；ProfileStore.Save 加锁 + 清理 `.tmp`；远端循环 `trap` 兜底清理临时文件
- **M** 合并两处导航方法为 `NavigateCoreAsync`；字节/速度格式化统一到 `SizeFormatter`；`ProcessRunner` 超时改用 `WaitAsync`（消除悬空计时器）；本地 CRUD 错误处理抽 `RunLocalAsync`；共享 `SshOptions`；`RemoteOps` 抽 `Cd()` 前缀；删除死类型 `LocalFileEntry`

## [0.2.0] - 2026-08-08

### 新增
- **文件夹/文件图标优化**：按类型区分（图片/压缩包/代码/文档/音频/视频/通用），Segoe MDL2 字形 + 配色
- **传输测速**：文件传输实时显示速度（轮询目标文件大小），完成后显示平均速度；文件夹显示平均速度
- **本地右键压缩/解压**：`.zip` 压缩与解压（.NET 内置 ZipArchive，正确处理中文文件名）
- 传输队列目标路径显示（"↑ 上传「x」 → /path"），状态栏同步
- `build-dist.cmd` 一键打包 + 初始化 git 仓库与开发文档

### 修复
- 常驻会话远程循环改用 `bash -c "$(echo b64 | base64 -d)"`（管道方式导致循环立即 EOF、会话失效）

## [0.1.0] - 2026-08-07

初始可用版本。轻量级 WinSCP 风格 Windows↔Linux 文件管理器。

### 新增
- 双栏文件管理（本地/远端）、路径栏、后退/前进/上级、显示隐藏
- 系统 OpenSSH 传输（复用 `~/.ssh/config`，含 ProxyJump 跳板、密钥登录）
- **通过脚本登录**：执行登录 .ps1 一次性模式验证连通 + 自动提取 `$Target`/`$WorkDir`
- 传输队列：上传/下载（含文件夹递归）、逐项取消、完成/总数统计
- 远端归档：右键 tar.gz / zip 压缩与解压（远端 tar/unzip）
- 内置远端文本查看器（编码切换、用默认程序打开、保存）
- 连接配置持久化（`%AppData%\WinLinScp\profiles.json`）
- `-selftest` 无头自检（服务层连真实主机验证）
- 单文件发布（`dist\WinLinScp.exe`）+ `build-dist.cmd` 一键打包

### 性能
- 列目录改为单进程 `find -printf`（原逐文件 stat/base64）
- 目录列表缓存：返回/后退命中秒开，后台静默刷新

### 修复
- WPF selftest 同步阻塞死锁（改线程池执行）
- scp 远端路径引号错误（改为裸 `alias:path` 拼接）
- `File.Replace` 首次保存失败（目标不存在时改用 Move）
- tar 解压路径错误（用归档完整路径）
